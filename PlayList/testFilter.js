const list = ["hello", "keep me alive", "adios"]
const TEXT = "keep";
const filter = (TEXT) => {
    const myFilter = list.filter(function (str) {return str.includes(TEXT); })
    console.log("HOLAA")
}

const myFilter = list.filter(function (str) {return str.includes(TEXT); })
    console.log("HOLAA")